pdfplumber
python-docx
openpyxl
python-pptx
scikit-learn
numpy
chromadb
tqdm
requests
fastapi
uvicorn

# 낱말 부분일치만으로는 문법을 못 구별한다("정해지다"의 활용형 "해지"를
# 계약 해지 명사와 못 나눔 - product_facts.detect_intents 참고). 순수
# Python 확장이라 별도 바이너리 설치 없이 pip만으로 된다.
kiwipiepy

# 같은 (messages, model, ...)로 HCX를 다시 부르면 캐시에서 바로 돌려준다
# (scripts/hcx.py chat 참고). 개발 중 같은 질문을 반복 테스트하는 게
# 토큰 사용의 큰 부분이었다.
diskcache

# HCX가 Planner/검증 JSON을 홑따옴표·trailing comma·앞뒤 설명문 섞어서
# 낼 때가 있다(agent_v2/json_extract.py). 순수 문법만 고치고 의미는
# 건드리지 않는다 - 도구를 잘못 고른 것 같은 의미 오류는 여전히 Pydantic
# 검증과 Python 가드가 잡는다.
json-repair

# sentence-transformers는 로컬 임베딩 모델(LocalEmbeddingProvider,
# EMBEDDING_PROVIDER=sentence_transformers)에서만 필요하다. 개발 환경에서는
# huggingface.co 접근이 막혀 있어 검증하지 못했으니, 접근 가능한 환경에서만
# 설치/사용할 것. 기본 프로바이더(tfidf)는 이 패키지가 없어도 동작한다.
# sentence-transformers
